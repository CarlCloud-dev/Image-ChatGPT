# Image-ChatGPT 使用说明教程

这份教程面向普通使用者，按“安装、登录、生成、管理、打包、排错”的顺序说明。

## 1. 软件用途

Image-ChatGPT 是一个本地桌面工具，用来控制 ChatGPT 网页版生成图片。

它不是 API 工具，也不需要填写 OpenAI API Key。它会启动你电脑上的 Chrome，并使用你自己的 ChatGPT 登录态。

可以完成这些工作：

- 单个提示词生成图片
- 多个提示词批量排队生成
- 上传参考图做图生图
- 每张参考图填写独立提示词
- 选择预设风格
- 自动保存生成结果
- 查看任务队列历史
- 复制旧任务提示词重新使用

## 2. 首次运行

### 2.1 开发方式运行

在项目根目录运行：

```bat
npm.cmd install
npm.cmd start
```

也可以双击：

```text
run.bat
```

### 2.2 运行打包后的 exe

打包后的程序在：

```text
dist-electron/Image-ChatGPT-win32-x64/Image-ChatGPT.exe
```

这是目录版便携应用。使用时双击文件夹里的 `Image-ChatGPT.exe` 即可启动，但不要只拷贝这个 exe；需要保留整个 `Image-ChatGPT-win32-x64/` 文件夹。

## 3. 登录 ChatGPT

首次使用需要登录 ChatGPT。

1. 打开应用。
2. 点击右上方 `显示浏览器`。
3. 在打开的 ChatGPT 页面登录账号。
4. 登录成功后回到应用。
5. 点击 `隐藏浏览器`，浏览器会继续在后台运行。

登录态会保存在：

```text
browser_profile/
```

下次启动通常不需要重新登录。

## 4. 窗口、托盘和退出

点击应用右上角关闭按钮时，应用只会隐藏到系统托盘，后台任务仍会继续运行。

托盘图标支持：

- 左键：打开主界面
- 右键：打开菜单
- 右键菜单 `显示 ChatGPT 浏览器` / `隐藏 ChatGPT 浏览器`
- 右键菜单 `打开图片目录`
- 右键菜单 `退出应用并停止服务`

只有点击 `退出应用并停止服务` 才会真正关闭后台服务和浏览器自动化。

## 5. 单图生成

1. 选择 `单图生成`。
2. 在提示词输入框填写内容。
3. 选择生成比例。
4. 选择生成张数。
5. 点击 `加入队列`。

任务会进入下方任务队列，完成后会显示缩略图和保存按钮。

## 6. 批量生成

1. 选择 `批量生成`。
2. 每行写一个提示词。
3. 选择生成比例和每个提示词生成张数。
4. 点击 `加入队列`。

示例：

```text
一只在月光下弹钢琴的橘猫，赛博朋克霓虹风格
极简水墨山水，宋代风格
电影海报风格的未来城市夜景
```

软件会按队列顺序依次提交到 ChatGPT。

## 7. 图生图

图生图模式适合用参考图生成变体。

### 7.1 每张图独立任务

1. 选择 `图生图`。
2. 点击 `添加参考图`，或把图片拖进上传区域。
3. 任务模式选择 `每张图独立任务`。
4. 可填写上方通用提示词。
5. 点击某张缩略图，可给这张图填写独立提示词。
6. 点击 `加入队列`。

如果某张图没有独立提示词，会使用上方通用提示词。

### 7.2 多张图作为同一任务

1. 选择 `图生图`。
2. 添加多张参考图。
3. 任务模式选择 `全部图片作为同一任务`。
4. 上方填写通用提示词。
5. 每张图也可以填写说明。
6. 点击 `加入队列`。

这种模式会把多张参考图一起上传到同一个 ChatGPT 任务里。

## 8. 预设风格

点击 `选择风格` 可打开预设风格面板。

选择后，软件会在发送给 ChatGPT 的完整提示词中追加对应风格关键词。

任务队列里显示的是用户原始提示词，不会显示内部追加的 `创建图片:`、比例和风格关键词。

## 9. 任务队列

任务队列会显示：

- 序号
- 参考图 / 输出图缩略图
- 提示词和当前进度
- 状态
- 操作按钮

常用操作：

- `保存`：保存或打开生成结果
- `重试`：失败任务重新加入队列
- `复制提示词`：复制该任务的原始提示词，并填入当前输入框
- `清空已完成`：清除成功和失败任务，保留排队/运行中的任务
- `停止队列`：清空排队任务，正在运行的任务会继续跑完

任务历史会保存到：

```text
config/queue_state.json
```

如果退出时有任务仍在排队或运行，下次启动会标记为“上次退出时未完成”，不会自动继续执行旧任务。

## 10. 图片保存位置

默认图片保存到：

```text
output/
```

应用里可以点击：

- `保存目录`：修改图片保存目录
- `打开图片目录`：在资源管理器中打开当前目录

图片会按日期归档，例如：

```text
output/2026-07-08/
```

## 11. 新对话和上次对话

顶部有两个切换项：

- `新对话`
- `上次对话`

它们用于手动切换当前 ChatGPT 页面。

生成时不会自动新建页面，而是使用当前选择对应的 ChatGPT 页面。

## 12. 日志和排错

日志位置：

```text
logs/app.log
```

日志会记录：

- 应用启动
- Chrome 启动
- 调试端口
- 浏览器显示/隐藏
- 任务入队
- 生成成功/失败
- 应用退出

如果遇到问题，优先查看这个文件。

## 13. 常见问题

### 13.1 显示浏览器失败

可以尝试：

1. 从托盘右键退出应用。
2. 重新打开应用。
3. 再点击 `显示浏览器`。

如果仍失败，查看：

```text
logs/app.log
```

### 13.2 提示找不到输入框或发送按钮

通常是 ChatGPT 网页结构变了。

选择器配置在：

```text
config/selectors.json
```

更新对应 selector 后通常不需要改代码。

### 13.3 生成失败或没有图片

可能原因：

- ChatGPT 账号未登录
- ChatGPT 页面在登录/验证页面
- 提示词被拒绝
- 网页结构变化
- 网络或 ChatGPT 服务异常

建议点击 `显示浏览器` 查看页面实际状态。

### 13.4 打包失败，提示 browser_profile 被占用

说明打包目录里的应用或后台 Chrome 还在运行。

处理方法：

1. 右键托盘图标。
2. 点击 `退出应用并停止服务`。
3. 再重新执行打包。

## 14. 打包说明

默认打包：

```bat
npm.cmd run build
```

默认打包会保留打包目录里的运行态数据，适合自己继续使用。

如果本地没有 `node_modules/`，打包脚本会自动执行 `npm.cmd install`；首次打包需要联网下载依赖。

默认产物是目录版便携应用。如果要发给别人，请压缩并发送整个 `dist-electron/Image-ChatGPT-win32-x64/` 文件夹。

干净打包：

```bat
npm.cmd run build:clean
```

干净打包不会带登录态、输出图片和队列历史，适合发给别人或做干净备份。

如果需要真正的单文件便携 exe，可以尝试：

```bat
npm.cmd run build:portable
```

只有执行这个命令并成功生成 portable 产物后，才是一个 exe 文件给别人用。

## 15. 注意事项

- 不要直接关闭 ChatGPT 浏览器窗口，需要隐藏时用应用按钮或托盘菜单。
- 本工具依赖 ChatGPT 网页版，网页变化可能导致自动化失效。
- 本工具不保存你的账号密码，只复用 Chrome 登录态。
- 本工具不是 OpenAI 官方项目。
